{
  "name": "cycling-program-gen",
  "version": "1.0.0",
  "description": "To generate the files",
  "license": "ISC",
  "author": "",
  "type": "module",
  "main": "generate-all.ts",
  "scripts": {
    "start": "bun run generate-all.ts",
    "generate": "bun run generate-all.ts",
    "generate:8weeks": "bun run generate-all.ts programs/8_weeks_road_builder",
    "generate:recoveries": "bun run generate-all.ts programs/recoveries",
    "generate:readme": "bun run generate-all.ts -- --detailed-readme",
    "migrate:config": "bun run scripts/migrate-to-config.ts",
    "format": "biome format --write .",
    "format:check": "biome format .",
    "lint": "biome lint --write .",
    "lint:check": "biome lint .",
    "check": "biome check --write .",
    "check:ci": "biome check .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.2.5",
    "@types/node": "^24.7.1",
    "@types/xml2js": "^0.4.14",
    "bun": "^1.2.4"
  },
  "dependencies": {
    "canvas": "^3.2.0",
    "xml2js": "^0.6.2"
  }
}
